resources:
  - ../../base

commonLabels:
  env: dev

patchesStrategicMerge:
  - deployment.yaml
  - ingress.yaml

patches:
  - target:
      kind: Ingress
      name: frontend
    patch: |-
      - op: add
        path: "/spec/rules/0/host"
        value: ssr-o.azure.amsterdam.nl
